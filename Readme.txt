To start the Express server, install node.js, then use nodemon or node and run in your console:
nodemon express

This will open up the server at localhost:3000.

To reach the app, type in your browser:
http://localhost:3000/app/#